# 데모/스크린샷 빌드 전용 (`npm run build:demo`)
#
# 실기기·AVD에서 예시 데이터로 스크린샷을 찍기 위한 모드입니다.
# API URL 등 나머지 값은 `.env` 에서 그대로 상속받고, 여기서는 목 데이터만 켭니다.
# 시크릿이 없으므로 이 파일은 커밋해도 됩니다.
#
# 운영 빌드(`npm run build`)는 MODE=production 이라 코드 레벨에서 목이 강제로 꺼집니다.
VITE_USE_MOCK=true
